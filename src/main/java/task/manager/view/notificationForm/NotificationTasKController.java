package task.manager.view.notificationForm;

import javafx.fxml.FXML;
import javafx.scene.control.*;

public class NotificationTasKController {
    // кнопка продляить : журнал -> обновить задачу + ту удалить из файла и новую записать в файл
    // кнопка отложенный : вызов окна редактирования, (меняем  ее в журнале)
    // отменненный меняем статус в журнале , отмененный при считываниии не считывать, или проверять на отмененнный
    // тк смысла их создавать нет
}
